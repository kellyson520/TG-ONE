# 完善日志优先级显示 (Enhance Priority Display) - 任务报告

## 摘要 (Summary)
成功优化了系统日志及指令反馈中的优先级显示。不再仅显示数字，而是将其转换为带有语义描述和 Emoji 的友好格式。

## 架构变更 (Architecture Refactor)
- **新增工具类**: 创建了 `core/helpers/priority_utils.py`，统一管理优先级到描述的映射逻辑。
- **监听器优化**: 修改 `listeners/message_listener.py`，在消息写入队列时记录更清晰的优先级信息。
- **指令优化**: 修改 `handlers/priority_handler.py`，使 `/set_priority` (alias `/vip`) 的反馈更直观。

## 验证结果 (Verification)
- **日志输出示例**: 
  `✅ [监听器] 普通消息已写入队列: ... 优先级=普通 (10) ✅ ...`
  `🚀 [监听器] 手动下载任务已写入队列: ... 优先级=最高优 (100) ⚡`
- **指令反馈**:
  `✅ 优先级已更新`
  `• 规则 ID: 123`
  `• 新优先级: 高优 (🚀) (50)`

## 交付产物 (Manual)
- 映射规则：
  - `>= 100`: `最高优 (⚡)`
  - `>= 90`: `极高 (🔥)`
  - `>= 50`: `高优 (🚀)`
  - `>= 10`: `普通 (✅)`
  - `== 0`: `低优 (🕒)`
  - `< 0`: `拥塞 (🐌)`
